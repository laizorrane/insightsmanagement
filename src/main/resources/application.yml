server:
  port: 8000
spring:
  application:
    name: Insights Management
  h2:
    console:
      enabled: true
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:insights-management
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create
      show-sql: true
  data:
    jdbc:
      dialect: h2